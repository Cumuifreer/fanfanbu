// @ts-ignore The actual Vite config lives in vite.config.js because it uses Node-only file APIs.
// This file stays as a thin TypeScript-facing re-export so the project keeps a single source of truth.
export { default } from './vite.config.js';
